# Contributing

Keep scheduler tie breakers explicit and deterministic. A new policy needs a motivating workload, an adverse test, documented assumptions, and unchanged behavior for existing policies. Build with warnings enabled and run CTest before proposing a change.

